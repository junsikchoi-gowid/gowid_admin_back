spring:
  redis:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 2

---
spring:
  profiles: dev
  redis:
    host: localhost
    port: 6379

---
spring:
  profiles: stg
  redis:
    host: TBD
    port: 6379

---
spring:
  profiles: prod
  redis:
    host: TBD
    port: 6379