package com.nomadconnection.dapp.core.domain.repository.kised;

import com.nomadconnection.dapp.core.domain.kised.ConfirmationFile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConfirmationFileRepository extends JpaRepository<ConfirmationFile, Long> {
}
